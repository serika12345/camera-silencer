plugins {
    id "com.android.application"
}

android {
    namespace "dev.masato.pixelcamerasilencer"
    compileSdk 36

    defaultConfig {
        applicationId "dev.masato.pixelcamerasilencer"
        minSdk 26
        targetSdk 36
        versionCode 1
        versionName "0.1.0"
    }
}
